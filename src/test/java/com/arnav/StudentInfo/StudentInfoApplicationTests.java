package com.arnav.StudentInfo;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.boot.test.autoconfigure.orm.jpa.TestEntityManager;
import org.springframework.boot.test.context.SpringBootTest;

//import com.arnav.StudentInfo.domain.Roles;

@SpringBootTest
class StudentInfoApplicationTests {

	@Autowired
//	private TestEntityManager entityManager;
	
	@Test
	public  void testCreateRoles() {
//		Roles roleAdmin = new Roles("Administrator");
//		Roles roleEditor = new Roles("Editor");
//		Roles roleVisitor = new Roles("Visitor");
//		
//		entityManager.persist(roleAdmin);
//		entityManager.persist(roleEditor);
//		entityManager.persist(roleVisitor);
		
	}

}
